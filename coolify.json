{
  "build": {
    "type": "nixpacks",
    "command": "pip install browser-use && playwright install",
    "include": ["README.md", "requirements.txt"]
  },
  "environment": {
    "OPENAI_API_KEY": "",
    "PLAYWRIGHT_BROWSERS_PATH": "/app/.playwright"
  }
}
